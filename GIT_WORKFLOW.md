# Git 协作说明

## 推荐分支策略

- `main`：始终保持可运行、可展示
- `dev`：团队日常集成分支，先合再进 `main`
- `feature/member-a-data`
- `feature/member-b-extraction`
- `feature/member-c-graph`
- `feature/member-d-ui`

如果你们不想开太多分支，至少保证：

- 日常开发不要直接往 `main` 里乱推
- 每次合并前先确认本地可运行

## 四人协作规则

- 成员 A 只改数据清洗与文本切分相关文件
- 成员 B 只改抽取结果与抽取说明相关文件
- 成员 C 只改本体、图谱与对齐相关文件
- 成员 D 只改查询、工作流、前端与展示相关文件

建议的协作顺序：

1. 先在 `docs/team_deliverables_interface.md` 确认接口
2. 再在各自分支开发
3. 开发完成后先合到 `dev`
4. `dev` 稳定后再合到 `main`

## 防止不同步

- 每个人开始改之前先 `git pull --rebase origin dev`
- 每个人提交都尽量只做一类事情
- 不要同时改同一个接口定义文件和实现文件
- 如果成员 C 改了 `schema.ttl` 的属性名，必须先更新接口文档，再通知成员 D
- 如果成员 B 改了实体字段名，必须同步更新示例和查询模板
- 如果成员 D 改了前端返回字段，必须同步更新后端接口说明

## 冲突处理

- 先保留接口文档里的约定，不要谁手快谁赢
- 先解决字段名和路径名，再解决样式和展示
- 合并冲突时优先保证 `main` 可运行
- 任何人如果改动了公共接口，必须在提交信息里写清楚

## 提交前必须检查

1. 页面是否还能打开
2. `app.py` 是否还能正常启动
3. 是否误提交了缓存文件、私钥、`.env`
4. 是否误提交了临时生成的大体积 JSON/TTL

## 推荐提交粒度

- 一次提交只做一类事情
- 不要把前端样式、后端逻辑、实验草稿混在一个提交里

例如：

- `feat: 新增 SPARQL 查询工具骨架`
- `feat: 新增普通问答页面布局`
- `docs: 更新成员 D 整合说明`
- `fix: 修复空 SPARQL 请求报错`

## 推荐协作方式

- 成员 B 改抽取逻辑时，不直接改成员 D 的前端展示
- 成员 C 改 schema 时，必须同步通知成员 D 更新查询模板
- 成员 D 如果改了接口字段，要同步写进文档
- 最好用 PR / 合并请求做一次人工确认，再进 `dev` 或 `main`

## 不建议提交到仓库的内容

- 本地调试缓存
- `.env`
- 私有 API Key
- 未清洗的巨大中间文件
- 临时截图或一次性测试输出
