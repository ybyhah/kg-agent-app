# 成员 D 下一步工作

## 你现在就可以做的事

1. 先把队友接口定下来

- 要求成员 B 输出 `entities.json`、`relations.json`、`attributes.json`
- 要求成员 C 输出 `schema.ttl`、`core.ttl`、`aligned.ttl`
- 在大规模写前端之前，先和成员 C 定死命名空间前缀、核心类名和属性名

2. 先把问题类型拆清楚

- 人物基础信息：字、号、籍贯、朝代、生卒年
- 关系类查询：父子、师承、交游、流派、开创
- 图结构查询：某人相关人物、人物路径、流派核心人物

3. 提前准备 SPARQL 模板

- 每一种高频问题至少准备一个模板
- 保留一个高级 SPARQL 直接执行入口
- 准备一组 few-shot 示例，后面接大模型生成 SPARQL 时直接复用

4. 提前确定问答工作流

- 先走确定性工具
- 再走基于 schema 的 SPARQL 模板
- 再尝试模型生成 SPARQL
- 最后才做兜底回答

5. 提前把界面壳子搭起来

- 普通问答输入框
- 高级 SPARQL 输入框
- 示例问题
- 结果展示区
- 当前 TTL 文件接入状态显示

## 整合时的检查清单

- 不要等最终数据都到齐了才开始做前端和路由
- 不要在没有书面字段约定的情况下硬编码队友的数据格式
- 所有查询逻辑都要保持可替换
- 主问答链路稳定之前，不要优先做可视化
